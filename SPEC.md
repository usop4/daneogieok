# Quiz01 / Quiz02 仕様書

## 0. 概要と共通仕様

本プロジェクトでは、似たような韓国語の意味・音を混同しやすい語彙を学ぶための 4 種類のクイズを提供する。

- Quiz01: ハングル語を見て、対応する漢字語を選ぶ
- Quiz02: 日本語語を見て、対応するハングル語を選ぶ
- Quiz03: ハングル語の動詞を見て、対応する日本語訳を選ぶ
- Quiz04: 日本語の動詞を見て、対応する韓国語を選ぶ

### 0.1 利用するデータ

- Quiz01,Quiz02 の問題データの元になる CSV: quiz01-data.js
- Quiz02 の選択肢候補を定義するデータ: quiz02-data.js
- Quiz03 の問題および選択肢候補の元になるデータ: quiz03-data.js

### 0.2 画面構成

各画面は以下の要素で構成される。

- 問題表示領域
- 選択肢ボタン領域
- 現在の回答表示
- 判定結果表示（answerPanel 内に正解/不正解メッセージを表示）
- 正解時ポップアップ
- 共通の見た目を持つカード型レイアウトと共通ナビゲーション
- 共通スタイルは quiz-common.css、共通ユーティリティは quiz-common.js に分離し、各ページから再利用できる構成とする。

### 0.3 操作方法

- マウスクリック: 選択肢を選ぶ
- 数字キー: 1〜7 の番号で選択肢を選ぶ
- Enter: 次の問題へ進む
- 正解時にはポップアップ表示後、続けて次の問題へ進める
- Quiz01 / Quiz02 では、問題の出題順も 1 巡分をシャッフルして順に進める
- Quiz03 / Quiz04 では、判定後に韓国語ラベルを表示し、正解時は次の問題へ自動遷移する流れを採用する
- 共通の見た目は quiz-common.css を用いて統一し、quiz-common.js で DOM 取得・エスケープ・シャッフル・統計更新・空テーブル表示などを共通化する

### 0.4 判定ルール

- 1 問題は複数文字からなる答えを持つ
- 各文字ごとに 1 つの選択肢を決める
- すべての列に選択が入ると自動判定される
- 正解時はポップアップを表示して次の問題へ進める
- 不正解時は answerPanel に結果メッセージを表示する
- 正解数・不正解数の統計を表示する
- Quiz02 では、選択肢の順番をランダム化して正解位置が固定されないようにする
- Quiz01 / Quiz02 では、問題の出題順も 1 巡分をシャッフルして順に進め、短い間隔で同じ問題が重なりにくいようにする
- Quiz03 / Quiz04 では、判定前はラベルを隠し、判定後にのみ見えるようにする
- 各クイズページ下部に、他のクイズへ移動できる簡易ナビゲーションを配置する

## 1. Quiz01 漢字語（韓→日）

### 1.1 目的

ハングル語を見て、その単語に対応する漢字語を選ぶ。

### 1.2 画面

- quiz01.html を開く
- 左側にクイズ本体を表示する
- 右側に「漢字語データ」入力欄を表示する

### 1.3 問題生成

- quiz01-data.js に記載された CSV 形式データを読み込む
- 1 行ごとに 1 問題として扱う
- ハングル語と漢字語の対応関係をもとに問題を作る
- 問題の出題順は、全体を 1 巡分シャッフルした順番で進める

### 1.4 データ入力と絞り込み

- 右側のテキストエリアに CSV を直接編集できる
- 「CSVを適用」ボタンで変更内容を反映する
- 子音の頭文字で問題を絞り込める
  - 例: ㄱ, ㄴ, ㄷ, ㄹ, ㅁ, ㅂ, ㅅ, ㅇ, ㅈ, ㅊ, ㅋ, ㅌ, ㅍ, ㅎ

### 1.5 内部辞書

- ハングル 1 文字ごとに、対応する漢字候補の集合を内部で生成する
- 1 列ごとに候補を並べてボタン表示する
- 1 列につき 1 つの漢字を選ぶ

### 1.6 判定と操作

- 選択後はその列に選択済み状態を表示する
- すべての列が埋まると自動判定する
- 正解時はポップアップを出し、次の問題へ進める
- 不正解時は answerPanel に結果メッセージを表示する

## 2. Quiz02  漢字語（日→韓）

### 2.1 目的と概要

- 日本語語を見て、その語に対応するハングル語を選ぶ。
- Quiz02 は Quiz01 の流れをベースにしつつ、逆向きの問題形式である。

### 2.2 画面

- quiz02.html を開く
- 日本語の語を問題として表示する
- その語に対応するハングル候補をボタンで表示する

### 2.3 問題生成

- quiz01-data.js のデータを元に問題を生成する
- 日本語語を問として表示し、対応するハングル語を選択させる

### 2.4 選択肢生成

- quiz02-data.js に定義された「似ているハングル群」を利用する
- 正解文字を含め、類似した文字候補を混ぜて選択肢を生成する
- 1 問あたり最大 7 個の選択肢を表示する
- 文字の近さに応じて候補が組み合わされるように構成する

### 2.5 判定と操作

- 各列に対して候補を選択する
- すべての列が埋まると自動判定する
- 正解時はポップアップを表示し、次の問題へ進める
- 不正解時は answerPanel に結果メッセージを表示する
- 選択肢の順番はランダムに並べ、正解の位置が固定されないようにする
- 問題の出題順も 1 巡分をシャッフルして進め、同じ問題が短い間隔で重なりにくくする

## 3. Quiz03 動詞（韓→日）

### 3.1 目的

文字数が多い韓国語の動詞を覚えやすいように、表示している韓国語の動詞に対し、共通点のある選択肢から日本語訳を選ぶ

### 3.2 画面

- quiz03.html を開く
- 問題文としてハングル語を表示する
- 選択肢として日本語訳や近い意味を表示する

### 3.3 問題生成

- quiz03-data.js のデータを元に問題を作る
- 1 行ごとに 1 問題として扱う
- 問題の出題順は、全体を 1 巡分シャッフルした順番で進める

### 3.4 選択肢生成

- quiz03-data.js のデータを元に選択肢を作る
- 生成済みの quiz03-option.txt と quiz03-option.js を読み込み、各ハングル語がどのグループに属するかを確認する
- 問題に対応するハングル語が 1 つ以上のグループに含まれている場合、そのグループ内の別エントリから候補を優先して選ぶ
- 同じ単語が複数グループに属していても構わず、すべての関連グループから候補を収集する
- quiz03-exclude.txt に記載された語は、グループ候補を集めた後の最終候補から除外する
- 後方一致の語尾グループは、さらに先頭文字の子音ファミリーごとに分割してグループ名を作る
  - 例: suffix_하다_ㄱ系, suffix_하다_ㄷ系
- 選択肢生成では、正解を 1 個含めたうえで、関連グループ由来の候補を優先して埋め、足りない場合のみ全体の候補からフォールバックする
- 候補選択前に、出題語・所属グループ・各グループ内単語をコンソールに出力する
- 1 問あたりの選択肢数は 4 個程度を基本とする
- 選択肢の本文は 18px、判定後に表示する韓国語ラベルは 16px とする

### 3.5 判定と操作

- 選択肢を選んで判定する
- 正解時・不正解時とも、選択肢ボタンの日本語訳に韓国語の動詞を追加表示する
- 判定前は韓国語のラベルを非表示にし、判定後にのみ表示する
- 判定後は正解・不正解の見た目を切り替え、正解は緑色、不正解は赤色で強調する
- 韓国語ラベルの文字サイズは見やすいように調整し、表示を明確にする
- 正解時にはそのまま次の問題へ進めるようにし、学習の流れを滑らかにする
- 正解・不正解で選択肢の見た目を分け、直感的に判断しやすくする

### 3.6 実装方針

- Quiz03 は quiz03-data.js のデータを読み込み、1 行ごとに 1 問題として扱う
- 問題文にはハングル語を表示し、選択肢には日本語訳を表示する
- 選択肢はグループ情報に基づいて関連語を優先しつつ、正解が必ず含まれるように生成する
- グループ情報の生成元は quiz03-option.txt / quiz03-option.js であり、HTML 側の JavaScript からそのまま利用できる構成にする
- 共通の見た目は quiz-common.css を使って統一し、各クイズページで同じ UI パターンを利用する
- 開発・調整時には、対象語の所属グループとグループ内単語をコンソールに出して確認できるようにする
- Quiz03 / Quiz04 の選択肢本文は 18px、判定後に表示する訳ラベルは 16px とする

### 3.7 補助スクリプト

- generate_quiz03_option.py: quiz03-data.js の内容をもとに、選択肢候補のグルーピングを行う
- 生成結果は quiz03-option.txt と quiz03-option.js の 2 形式で出力し、テキスト確認用とブラウザ読み込み用を分ける
- グルーピングでは、語尾一致の分類に加えて、先頭文字の子音ファミリーごとの再分割を行う
- quiz03-exclude.txt に記載された語は、選択肢生成時の最終候補から除外する
- 1 語が複数グループに属していてもよいとし、グループ数が増えても構わない前提で生成する
- これらのスクリプトは、HTML 側の JavaScript がそのまま扱いやすいようにデータ整形を行う

## 4. Quiz04 動詞（日→韓）

### 4.1 目的

日本語の動詞を見て、対応する韓国語を選ぶ。Quiz03 の逆向きの問題形式であり、意味の近い語を混ぜた選択肢から正解を選ぶことで、韓国語の動詞表現を覚えやすくする。

### 4.2 画面

- quiz04.html を開く
- 問題文として日本語の動詞を表示する
- 選択肢として韓国語の動詞を表示する
- Quiz03 と同じく、選択肢ボタンと判定結果表示を用いる

### 4.3 問題生成

- quiz03-data.js のデータを元に問題を作る
- 1 行ごとに 1 問題として扱う
- 各行の日本語訳を問題文として表示し、対応するハングル語を答えとする

### 4.4 選択肢生成

- Quiz03 と同様に、生成済みの quiz03-option.txt / quiz03-option.js を参照して、問題に関連するグループを特定する
- 出題された日本語の意味に紐づく韓国語が所属するグループを取得し、そのグループ内の他単語を候補として優先して選ぶ
- 1 語が複数グループに属していてもよく、複数の関連グループから候補を収集する
- quiz03-exclude.txt に記載された語は、グループ候補を集めた後の最終候補から除外する
- 正解を 1 個含めたうえで、関連グループ由来の候補を優先して 4 択程度に整える
- 候補不足時のみ、全体の単語からフォールバック候補を追加する
- 開発時は、出題語・所属グループ・各グループ内語をコンソールに出して確認する

### 4.5 判定と操作

- 選択肢を選んで判定する
- 正解時・不正解時とも、韓国語の表示を見やすいように強調する
- 判定前は韓国語ラベルを非表示にし、判定後にのみ表示する
- 正解時は緑色、不正解時は赤色で選択肢を見分けやすくする
- 正解数・不正解数の統計を表示する
- 選択肢の本文は 18px、判定後に表示する韓国語ラベルは 16px とする

### 4.6 実装方針

- Quiz04 は Quiz03 の構成を踏襲し、問題文は日本語、選択肢は韓国語として扱う
- グルーピング済みのデータを再利用し、意味的に近い韓国語を選択肢として優先する
- 共通 UI は quiz-common.css を使用し、Quiz03 と同じ見た目・操作感で統一する
- 必要に応じて、Quiz03 と同じログ出力・デバッグ支援を行う

### 4.7 補助スクリプト

- Quiz04 では、Quiz03 と同じグルーピング生成スクリプトを利用する
- 生成された quiz03-option.txt / quiz03-option.js を、Quiz04 の選択肢生成でも再利用する
- 生成ルールは Quiz03 と同じく、語尾一致と子音ファミリーごとの再分割を基本とする




