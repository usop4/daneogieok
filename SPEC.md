# Quiz 仕様書

## 0. 概要

似たような韓国語の意味・音を混同しやすい語彙を学ぶための 4 種類のクイズと、進捗確認用の Stats 画面を提供する。

- Quiz01: 対象=漢字語、方向=韓→日、問題データ=quiz01-data.txt、選択肢データ=内部辞書（自動生成）、判定単位=複数列・全列自動判定、正誤表示=ポップアップ + answerPanel
- Quiz02: 対象=漢字語、方向=日→韓、問題データ=quiz01-data.txt、選択肢データ=quiz02-data.js、判定単位=複数列・全列自動判定、正誤表示=ポップアップ + answerPanel（赤/緑枠）
- Quiz03: 対象=動詞、方向=韓→日、問題データ=quiz03-data.txt、選択肢データ=quiz03-option.txt/js、判定単位=単一選択、正誤表示=hintText（赤/緑）
- Quiz04: 対象=動詞、方向=日→韓、問題データ=quiz03-data.txt、選択肢データ=quiz03-option.txt/js、判定単位=単一選択、正誤表示=hintText（赤/緑）

## 1. データファイル

- quiz01-data.txt: `ハングル語,漢字語[,補足]` の CSV。Quiz01/02 の問題元
- quiz02-data.js: Quiz02 用「似ているハングル群」定義（選択肢生成に使用）
- quiz03-data.txt: `ハングル語,日本語訳[,例文...]` の CSV。Quiz03/04 の問題元。3 列目以降は例文で選択肢生成には使わない
- quiz03-option.txt / quiz03-option.js: Quiz03/04 共通の選択肢グルーピングデータ（テキスト確認用・ブラウザ読込用）
- quiz03-exclude.txt: Quiz03/04 の選択肢候補から除外する語
- word.txt: 元データ。generate_quiz0103_data.py の入力

## 2. 画面構成（共通）

- 問題表示領域、選択肢ボタン領域
- Quiz01/02: answerPanel に正解/不正解メッセージ、正解時ポップアップ表示
- Quiz03/04: 問題下のヒント領域（hintText）に判定結果を表示（ポップアップなし）。CSV 3 列目以降に例文があれば同領域に併記
- 共通のカード型レイアウトと下部ナビゲーション（Quiz01/02/03/04/Stats へのリンク）
- 共通スタイル: quiz-common.css、共通ユーティリティ（DOM 取得・エスケープ・シャッフル・統計更新・空テーブル表示等）: quiz-common.js
- Quiz03/04: 選択肢本文 18px、判定後に表示する訳/韓国語ラベルは 16px

## 3. 操作方法（共通）

- マウスクリックで選択肢を選ぶ
- 数字キー: Quiz01/02 は 1〜7、Quiz03/04 は 1〜4 で選択肢を選び判定
- Enter: 次の問題へ進む（Quiz01/02 は正解ポップアップの確定にも使用。Quiz03/04 は判定後のみ有効）
- Quiz01/02: すべての列が埋まると自動判定。正解時はポップアップ表示後、Enter または再操作で次の問題へ
- Quiz03/04: 判定後に「次の問題」ボタンまたは正解ボタン再押下で次の問題へ進める

## 4. 判定・進捗管理ロジック（共通）

### 4.1 出題順
- 全クイズ共通: 1 巡分をシャッフルして出題し、同じ問題が短い間隔で重ならないようにする
- 起動時、localStorage 上に ok リストがあれば、その単語を questionOrder の末尾に配置する

### 4.2 統計・localStorage
- 正解数・不正解数・総回答数、正解/不正解の単語リストを localStorage に保存し、画面遷移後も statPill に反映する
- 回答のたびに、日付（YYYY-MM-DD）ごとの OK/NG 件数を localStorage に保存する。常に直近 7 日分のみ保持し、古い日付データは自動削除する

### 4.3 statPill クリック時の挙動（全クイズ共通）
1. 不正解単語一覧をコンソールに出力する
2. 不正解を優先する出題順（不正解履歴のある単語を先頭側、正解のみの単語を末尾側）に再構築する
3. 統計値（正解数・不正解数・総回答数）と単語ごとの outcomes を 0/空にリセットし、保存する

### 4.4 ng 履歴の扱い（全クイズ共通）
- statPill でリセットするまでは、一度不正解になった単語を後で正解し直しても ng 履歴を保持し、不正解優先の対象に残す

### 4.5 デバッグ用コンソール出力（共通ルール・内容はクイズごとに異なる）
- 不正解時: 対象単語を出力（全クイズ共通）
- Quiz03/04 選択肢生成時: 出題語・所属グループ・各グループ内単語を出力（4.5, 6章参照）

## 5. Quiz01: 漢字語（韓→日）

**目的**: ハングル語を見て、対応する漢字語を選ぶ。

- 画面: quiz01.html。左にクイズ本体、右に「漢字語データ」CSV 編集欄（テキストエリア＋「CSVを適用」ボタンで反映）。子音頭文字（ㄱ〜ㅎ）で問題を絞り込み可能
- 問題生成: quiz01-data.txt を 1 行 1 問として読み込む
- 選択肢: ハングル 1 文字ごとに対応する漢字候補の集合を内部辞書として生成し、列ごとに候補をボタン表示。1 列につき 1 つ選択
- 判定: 共通ルール（4章）に従う。正解ポップアップには `ハングル語 = 漢字語` を表示し、3 列目に補足情報があれば併記する

## 6. Quiz02: 漢字語（日→韓）

**目的**: 日本語語を見て、対応するハングル語を選ぶ（Quiz01 の逆方向）。

- 画面: quiz02.html。日本語語を問題文とし、対応するハングル候補をボタン表示
- 問題生成: quiz01-data.txt を用い、日本語語を問題、ハングル語を答えとする。3 列目の補足情報は正解ポップアップに併記
- 選択肢: quiz02-data.js の「似ているハングル群」を利用し、正解を含め類似文字を混ぜて最大 7 個生成。カナダラ順で表示
- 判定: 共通ルール（4章）に従う。不正解時は誤答列を赤枠、正解列を緑枠で表示する

## 7. Quiz03: 動詞（韓→日）

**目的**: 文字数の多い韓国語動詞を、共通点のある選択肢の中から日本語訳を選んで覚える。

- 画面: quiz03.html。問題文にハングル語、選択肢に日本語訳（+ 判定後は対応する韓国語動詞も併記）を表示
- 問題生成: quiz03-data.txt を 1 行 1 問として読み込む（第 1 列=ハングル語、第 2 列=日本語訳、第 3 列以降=例文）
- 選択肢生成:
  1. quiz03-option.txt/js を参照し、出題語が属するグループ（1 つ以上あり得る）を特定
  2. 該当グループ内の他エントリを優先候補として収集（複数グループに属する語も全グループから収集）
  3. quiz03-exclude.txt に含まれる語を最終候補から除外
  4. 正解を 1 個含め、グループ由来の候補を優先して 4 択程度に整形。不足時のみ全体からフォールバック
  5. 候補選択前に出題語・所属グループ・グループ内単語をコンソール出力
- 判定: 共通ルール（4章）に従う。判定前は韓国語ラベルを非表示、判定後に表示。正解=緑、不正解=赤で選択肢を強調。正解時はそのまま次の問題へ進行可能

## 8. Quiz04: 動詞（日→韓）

**目的**: 日本語の動詞を見て対応する韓国語を選ぶ（Quiz03 の逆方向）。

- 画面: quiz04.html。問題文に日本語動詞、選択肢に韓国語動詞を表示。UI・操作感は Quiz03 に準拠
- 問題生成: quiz03-data.txt を用い、日本語訳を問題文、ハングル語を答えとする。3 列目以降の例文は判定後 hintText に表示
- 選択肢生成: Quiz03 と同一ロジック（quiz03-option.txt/js を参照したグループベース抽出 → quiz03-exclude.txt で除外 → 正解含め 4 択程度に整形 → 不足時フォールバック）。カナダラ順で表示
- 判定: Quiz03 と同一（共通ルール4章＋判定前非表示/判定後緑赤表示）

## 9. 選択肢グルーピング生成（Quiz03/04 共通の補助スクリプト）

- **generate_quiz0103_data.py**: word.txt（CSV）から quiz01-data.txt と quiz03-data.txt を生成する
  - 第 1 列が `다` で終わる行 → Quiz03 用データに分類
  - 動詞に該当せず、第 1 列と第 2 列の文字数が一致する行 → Quiz01 用データに分類
  - それ以外・空行・コメント行・2 列未満の行は出力しない
  - 第 3 列以降は補足情報／例文として分類先に引き継ぐ
- **generate_quiz03_option.py**: quiz03-data.txt を元に選択肢候補をグルーピングし、quiz03-option.txt（確認用）と quiz03-option.js（ブラウザ読込用）を出力する
  - 語尾一致によるグループ分類に加え、後方一致グループをさらに先頭文字の子音ファミリー別に再分割する（例: `suffix_하다_ㄱ系`, `suffix_하다_ㄷ系`）
  - 1 語が複数グループに属することを許容する
  - quiz03-exclude.txt による除外はこのスクリプトでは行わず、quiz03.html / quiz04.html 側の選択肢生成時に適用する

## 10. Stats 画面（quiz-stats.html）

**目的**: Quiz01〜04 の学習進捗を、過去 7 日分の日次 OK/NG 件数として一覧確認する。

- 日付ごとに Quiz01〜04 の OK/NG 件数をテキスト表示。新しい日付を上（降順）、OK は緑文字、NG は赤文字
- データは quiz-common.js の共通ヘルパーで localStorage に保存・参照（キー: 日付単位、値: quiz01〜04 ごとの OK/NG カウント）。直近 7 日分のみ保持
- 下部に Quiz01/02/03/04/Stats への相互リンクを配置